@page
@model FarmManagementApp.Pages.UserProfile.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].RoleGu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserProfile[0].UserGu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UserProfile) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleGu.Guid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserGu.Guid)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.UserGuid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.UserGuid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.UserGuid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
